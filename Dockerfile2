FROM quay.io/centos/centos:stream9

RUN mkdir -p /opt/aes/
COPY *.sh /opt/aes

RUN chmod +x /opt/aes/*.sh
RUN dnf install -y python3 python3-pip tmux && dnf clean all

ENTRYPOINT ["/opt/aes/entrypoint.sh"]